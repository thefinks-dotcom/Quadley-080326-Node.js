// STATIC tenant config for Quadley — committed to repo, never auto-generated.
// Used as the canonical source of truth for Quadley builds.
// app.config.js copies this content into tenantBuild.generated.js during
// `TENANT=quadley npx expo prebuild` (or when no TENANT is set).
const BUILD_CONFIG = {
  tenant: "quadley",
  tenantName: "Quadley",
  primaryColor: "#1e3a5f",
  secondaryColor: "#c9cdd5",
};
module.exports = BUILD_CONFIG;
