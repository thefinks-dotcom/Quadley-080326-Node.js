// STATIC tenant config for Grace College — committed to repo, never auto-generated.
// Used as the source of truth for Grace College builds.
// app.config.js copies this content into tenantBuild.generated.js during
// `TENANT=grace_college npx expo prebuild`.
const BUILD_CONFIG = {
  tenant: "grace_college",
  tenantName: "Grace College",
  primaryColor: "#E05A20",
  secondaryColor: "#3E1B5E",
};
module.exports = BUILD_CONFIG;
